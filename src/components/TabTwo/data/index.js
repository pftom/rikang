import {
  handleQuestions,
  handleQuestion,
} from './qa_data.js';

import {
  handleAnswers,
  getLatestId,
  getBeRepliedName,
} from './answer_data.js';

export {
  handleQuestions,
  handleQuestion,
  handleAnswers,
  getLatestId,
  getBeRepliedName,
}